# Google Cloud Configuration (Required for production)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Ads Configuration (Automatically populated by Google Cloud)
# This environment variable is automatically set by Google Cloud Run/Cloud Functions
# when you configure GOOGLE_ADS_CONFIG as a secret environment variable
GOOGLE_ADS_CONFIG={"customer_id":"your-customer-id","developer_token":"your-developer-token","service_account_json":"{\"type\":\"service_account\",...}"}

# MCP Server Configuration (Non-sensitive)
MCP_SERVER_NAME=google-ads-mcp
MCP_SERVER_VERSION=1.0.0
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PATH=/mcp
PORT=8080

# PRODUCTION SETUP:
# 1. Create GOOGLE_ADS_CONFIG secret in Google Secret Manager containing:
#    {
#      "customer_id": "your-customer-id",
#      "developer_token": "your-developer-token",
#      "service_account_json": "{\"type\":\"service_account\",...}"
#    }
# 2. Configure Google Cloud Run/Cloud Functions to use GOOGLE_ADS_CONFIG as secret environment variable
# 3. Google Cloud will automatically populate the environment variable with the secret value
